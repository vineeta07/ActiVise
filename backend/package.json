{
  "name": "civiclens-contracts",
  "private": true,
  "version": "0.1.0",
  "description": "Smart contracts for CivicLens certificate anchoring",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "dotenv": "^16.4.5",
    "hardhat": "^2.24.1"
  },
  "dependencies": {
    "fp-ts": "^2.16.11",
    "io-ts": "^2.2.22"
  }
}
